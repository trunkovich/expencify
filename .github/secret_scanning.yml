paths-ignore:
  # Firebase client config often includes Google API keys that are expected to be
  # shipped with the app. We restrict the key in GCP instead of treating it as a secret.
  - "lib/firebase_options.dart"
  - "android/app/google-services.json"

